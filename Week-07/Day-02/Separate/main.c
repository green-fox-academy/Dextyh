#include <stdio.h>
#include "functions.h"

int main() {

    printf("Area of the circle: %d\n", circle_area(10));
    printf("Circumference of the circle: %d\n", circle_circumference(10));

    return 0;
}